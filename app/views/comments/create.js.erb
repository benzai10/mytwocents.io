$('#form_content').val('');
$('#form_mood_id').val('');
$('.mood-row i').removeClass('highlight');

$('.comments').html("<%= escape_javascript(render(@comments)) %>");
$('.cents').text("<%= @update_post %> cents");

$('.new_posts').load('/posts/nav_new_posts');
$('.happy-row').load('/posts/nav_counter');
